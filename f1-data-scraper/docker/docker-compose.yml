services:
    api:
        container_name: f1_stats_scraper
        build:
            context: ../
            dockerfile: docker/Dockerfile
        volumes:
            - ../:/app
        ports:
            - '5050:5000'
        environment:
            - FLASK_ENV=development
